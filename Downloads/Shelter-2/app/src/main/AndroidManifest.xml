<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shelter">

    <application
        android:name=".ShelterTrackerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shelter">
        <activity
            android:name=".RemoveAnimalActivity"
            android:exported="false" />
        <activity
            android:name=".AddAnimalActivity"
            android:exported="false" />
        <activity
            android:name=".ShelterDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ShelterListActivity"
            android:exported="false" />
    </application>

</manifest>